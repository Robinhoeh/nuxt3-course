#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../wait-port@1.0.4/node_modules/wait-port/bin/wait-port.js" "$@"
else
  exec node  "$basedir/../../../../../wait-port@1.0.4/node_modules/wait-port/bin/wait-port.js" "$@"
fi
